{
  "name": "saju",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --test --experimental-strip-types packages/*/test/*.test.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "dependencies": {
    "satori": "^0.29.0",
    "subset-font": "^2.5.0"
  }
}
